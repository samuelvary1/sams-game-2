<body>

	<% @characters.each do |f| %>
		<p>
			Name: <%= f.first_name + " " + f.last_name %><br>
			Rank: <%= f.rank %><br>
			Birthplace: <%= f.birthplace %><br>
			Role: <%= f.role %><br>
			Bio: <%= f.bio %>
		</p>
		
	<% end %>

</body>